/** Prefix all layers so we can find them in mapbox */
export const DECKGL_LAYER_PREFIX = "deckgl-";

/** Default viewport if none provided to MapProvider */
export const PACHAMA_HQ = {
  latitude: 37.7627,
  longitude: -122.5016,
  zoom: 14.5,
};
